import React, {useState} from "react";
import {IonPage, IonToast} from "@ionic/react";

const ShowToast = (props: any) => {

    //showToast = props.location.
/*
    return(
        <IonToast
        isOpen={showToast}
        onDidDismiss={() => setShowToast(false)}
        message={toastMessage}
        duration={3000}
        color={toastColor}/>
    ) */
    return(
        <IonPage></IonPage>
    )
}

export default ShowToast;